<h1>Edit Link</h1>

<%= render partial: "form", locals: { link: @link } %>

<div class="mt-4 flex gap-1 items-center">
  <%= link_to "Cancel", link_path(@link.short_code) %>
  or
  <%= button_to "Delete", link_path(@link.short_code), method: :delete, data: { turbo_confirm: "Are you sure?" } %>
</div>