<!DOCTYPE html>
<html>
  <head>
    <title>RailsUrlShortener</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="prose mx-auto mt-28 px-5">
      <%= tag.p alert, class: "bg-yellow-100 text-yellow-800 text-sm font-medium px-4 py-2 rounded" if alert %>
      <%= tag.p notice, class: "bg-blue-100 text-blue-800 text-sm font-medium px-4 py-2 rounded" if notice %>

      <nav class="flex items-center justify-between mb-12">
        <h1 class="m-0">URL Shortener</h1>
        <div class="flex gap-2 items-center">
          <% if user_signed_in? %>
            <%= link_to "Profile", edit_user_registration_path, class: "no-underline bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded transition duration-200" %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "no-underline bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded transition duration-200" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "no-underline bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded transition duration-200" %>
            <%= link_to "Sign up", new_user_registration_path, class: "no-underline bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded transition duration-200" %>
          <% end %>
        </div>
      </nav>

      <%= yield %>
    </main>
  </body>
</html>
